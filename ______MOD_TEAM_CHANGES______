Pra compilar novas versões é preciso copiar os seguintes arquivos da versão que a gente modificou:

interface.cc
Descriptor.h
basics.h
Makefile
TTLS/ (diretório)

além disso é necessario encapsular a seguinte linha no Chorus.cc :

Properties = HARD_RT;

ficando:

#ifdef LADSPAFLAG
    Properties = HARD_RT;
#endif
